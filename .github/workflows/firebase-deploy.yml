name: Deploy to Firebase Hosting

# Deploys public/ to Firebase Hosting (live channel) on every push to main.
# Auth: a Google service-account JSON stored in the FIREBASE_SERVICE_ACCOUNT secret.
# See SETUP.md → "Continuous deployment" for how to create both.
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          projectId: ${{ vars.FIREBASE_PROJECT_ID }}
          channelId: live
